{
  "framework": "vite",
  "buildCommand": "cd staff-portal && npm install && npm run build",
  "outputDirectory": "staff-portal/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
